import './styles.scss';

const Spectacles = () => {
  const cssClass = 0;

  return (
    <main className={cssClass}>
      <h1 className="posts-title">Spectacles</h1>
    </main>
  );
};

export default Spectacles;
