import './styles.scss';

const Galery = () => {
  const cssClass = 0;

  return (
    <main className={cssClass}>
      <h1 className="posts-title">Galerie</h1>
    </main>
  );
};

export default Galery;
