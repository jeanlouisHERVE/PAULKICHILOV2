import './styles.scss';

const Exhibitions = () => {
  const cssClass = 0;

  return (
    <main className={cssClass}>
      <h1 className="posts-title">Expositions</h1>
    </main>
  );
};

export default Exhibitions;
