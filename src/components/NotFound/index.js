import './styles.scss';

const NotFound = () => {
  const cssClass = 0;

  return (
    <main className={cssClass}>
      <h1 className="posts-title">Error 404</h1>
    </main>
  );
};

export default NotFound;
