import './styles.scss';

const Monotypes = () => {
  const cssClass = 0;

  return (
    <main className={cssClass}>
      <h1 className="posts-title">Monotypes</h1>
    </main>
  );
};

export default Monotypes;
